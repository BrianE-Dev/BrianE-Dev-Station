# BrianE-Dev Backend

Express REST API for the BrianE-Dev platform.

## Run

```bash
npm install
npm run dev
```

The API defaults to `http://localhost:4000/api`.

## Local test account

The in-memory seed includes an admin account for local testing:

* Email: `admin@Brainedev.com`
* Password: `admin123`
